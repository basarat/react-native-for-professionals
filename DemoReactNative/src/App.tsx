import React from 'react';

import {
  StyleSheet,
  View,
} from 'react-native';

const styles = StyleSheet.create({
  root: {
    backgroundColor: '#1893F8',
    height: '100%',
    width : '100%'
  },
});

const App = () => {
  return (
    <View style={styles.root}>
    </View>
  );
};

export default App;